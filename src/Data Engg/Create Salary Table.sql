CREATE TABLE Company.Salary (
    EmpID INT,
    Name VARCHAR(255),
    JoinDate DATE,
    HourlyRate DECIMAL(10, 2), -- Assuming a decimal data type for hourly rate with precision 10 and scale 2
    BaseSalary DECIMAL(10, 2)  -- Assuming a decimal data type for base salary with precision 10 and scale 2
);
